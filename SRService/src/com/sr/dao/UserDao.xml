<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sr.dao.UserDao">

	<insert id="createUser" parameterType="com.sr.dao.UserDto">
		INSERT INTO users (
		username, password, enabled, 
		created_time,updated_time,created_by, updated_by)
		VALUES 
		(#{user.userName}, #{user.password},#{user.enabled}, #{user.createdTime},
		#{user.updatedTime}, #{user.createdBy}, #{user.updatedBy})
	</insert>

	<insert id="createUserAuthority" parameterType="com.sr.dao.UserAuthorityDto">
		INSERT INTO authorities (
		username,authority, 
		created_time,updated_time,created_by, updated_by)
		VALUES
		(#{userAuthority.userName}, #{userAuthority.authority},
		#{userAuthority.createdTime},#{userAuthority.updatedTime}, #{userAuthority.createdBy}, #{userAuthority.updatedBy})
	</insert>

	<!-- <update id="vote" parameterType="com.sr.dao.UserDto"> UPDATE thing 
		set rate=rate+#{rate}, number_of_votes=number_of_votes+1 WHERE id = #{id} 
		</update> <select id="search" resultType="com.sr.dao.UserDto"> SELECT id, 
		name, description, rate, number_of_votes numberOfVotes, created_time createdTime, 
		updated_time updatedTime, created_by createdBy, updated_by updatedBy FROM 
		thing WHERE description like #{phrase} or name like #{phrase} </select> -->
</mapper>    